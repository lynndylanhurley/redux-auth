import { expect } from "chai";

describe("authenticate", () => {
  describe("success", () => {
    it("should call authenticate and then update the store", () => {
      expect(true).to.equal(true);
    });
  });
});
